name: T - secret inheritance caller (temporary)

# ⛔ 臨時驗證用（驗完即刪）。以 secrets: inherit 呼叫 t-secret-callee.yml。
on:
  workflow_dispatch:

jobs:
  call:
    uses: ./.github/workflows/t-secret-callee.yml
    secrets: inherit
